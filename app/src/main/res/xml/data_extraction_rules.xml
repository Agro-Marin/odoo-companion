<?xml version="1.0" encoding="utf-8"?>
<!-- The config store holds a device Bearer token and the outbox holds unsent
     location and call data. Neither belongs in a cloud backup or on a phone the
     user transferred to, so both transports are excluded outright. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
